name: CI/CD

on:
  push:
    branches: [master] 

permissions:
  contents: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push development Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/laravel-app:${{ github.sha }}
          ${{ secrets.DOCKERHUB_USERNAME }}/laravel-app:latest

    - name: Build and push production Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./Dockerfile.prod
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/laravel-app:prod

    # ... resto do arquivo permanece o mesmo ...
    # Se você precisar atualizar arquivos Kubernetes, descomente e ajuste as próximas etapas

    # - name: Setup Kustomize
    #   uses: imranismail/setup-kustomize@v1
    #   with: 
    #     kustomize-version: "3.6.1"

    # - name: Update Kubernetes resources
    #   env:
    #     DOCKER_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
    #   run: |
    #     cd k8s
    #     kustomize edit set image laravel-app=$DOCKER_USERNAME/laravel-app:${{ github.sha }}

    # - name: Commit
    #   run: |
    #     git config --local user.email "action@github.com"
    #     git config --local user.name "GitHub Action"
    #     git commit -am "Bump docker version"

    # - name: Push
    #   uses: ad-m/github-push-action@master
    #   with:
    #     github_token: ${{ secrets.GITHUB_TOKEN }}
    #     repository: Vitorir/laravel